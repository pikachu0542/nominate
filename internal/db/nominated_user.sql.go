// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: nominated_user.sql

package db

import (
	"context"
)

const addNominatedUser = `-- name: AddNominatedUser :one
INSERT INTO nominated_user (username, nomination_id)
VALUES ($1, $2)
RETURNING id, username, nomination_id
`

type AddNominatedUserParams struct {
	Username     string
	NominationID int32
}

func (q *Queries) AddNominatedUser(ctx context.Context, arg AddNominatedUserParams) (NominatedUser, error) {
	row := q.db.QueryRow(ctx, addNominatedUser, arg.Username, arg.NominationID)
	var i NominatedUser
	err := row.Scan(&i.ID, &i.Username, &i.NominationID)
	return i, err
}

const listUsersInNomination = `-- name: ListUsersInNomination :many
SELECT id, username, nomination_id
FROM nominated_user
WHERE nomination_id = $1
ORDER BY username ASC
`

func (q *Queries) ListUsersInNomination(ctx context.Context, nominationID int32) ([]NominatedUser, error) {
	rows, err := q.db.Query(ctx, listUsersInNomination, nominationID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []NominatedUser
	for rows.Next() {
		var i NominatedUser
		if err := rows.Scan(&i.ID, &i.Username, &i.NominationID); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
